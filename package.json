{
  "name": "pdf-pack",
  "version": "0.0.0",
  "description": "pack to png from images",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "defaults": "^1.0.0",
    "image-size": "^0.2.3",
    "pdfkit": "^0.6.1"
  },
  "devDependencies": {
    "mocha": "^1.18.2"
  }
}
